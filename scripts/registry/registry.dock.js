

// scripts/registry/registry.dock.js

window.registerPanel = function(panelId, panelConfig) {
  console.warn(`⚠️ registerPanel() is a stub. Panel ID: ${panelId}`, panelConfig);
  // In production, this would bind the panel to the dock registry
};